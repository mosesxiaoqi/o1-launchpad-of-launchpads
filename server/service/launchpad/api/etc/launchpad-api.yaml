Name: launchpad-api
Host: 127.0.0.1
Port: 8888
Mode: ${SERVICE_MODE}
LaunchpadRpc:
  Endpoints:
    - 127.0.0.1:8080
Auth:
  SessionSecret: ${SESSION_SECRET}
  ChallengeTTLSeconds: 300
  AllowedOrigin: ${FRONTEND_ORIGIN}
